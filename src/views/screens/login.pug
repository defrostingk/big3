extends ../base.pug

block content
  .login__container
    include ../partials/title

    .login
      form.login__form
        input.login__id(type='text', placeholder='Id', required)
        input.login__password(
          type='password',
          placeholder='Password',
          required
        )
        button.login-btn Login
        span
        button.join-btn Join

    include ../partials/footer
